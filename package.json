{
  "name": "all-care-mint",
  "version": "1.0.0",
  "private": true,
  "description": "All Care Mint — Service Booking Platform Monorepo",
  "scripts": {
    "build": "pnpm --filter \"*\" build",
    "lint": "pnpm --filter \"*\" lint",
    "test": "pnpm --filter \"*\" test"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "prettier": "^3.2.5"
  }
}
